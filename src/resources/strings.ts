import LocalizedStrings from "localized-strings";

const strings = new LocalizedStrings({
    ptBR: {
        helloWorld: "Hello World",
        sayHelloWorld: "Diga Hello World",
    },
});

export default strings;
